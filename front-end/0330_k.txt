2021.03.30 강은비

실제 서비스 할 때는 bulid 사용

react를 통해 실제로 컴포넌트 만드는 방법

ex. react에서 컴포넌트를 만들때 쓰이는 기본 형식
class App extends Component {
   render() {
      return (
         <div className="App">
            Hello, React!!
         </div>
      );
   }
}

1. 컴포넌트는 최상의 헤더로 시작해야한다.
2. render() 는 리엑트에서 컴포넌트를 만들 때 꼭 넣어야 하는 요소이다.


리팩토링
{this.props.00000} 을 사용해 원하는 요소를 각자 다르게 설정할 수 있음. 약간 오버로딩? 오버로드 같은 느낌.